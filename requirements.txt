# Python >= 3.10 required

openai>=1.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
rich>=13.0.0
openpyxl>=3.1.0

# Windows 下需要 readline 支持（Linux/Mac 内置）
# pyreadline3>=3.4.1   # 取消注释以在 Windows 启用 readline 历史

# 记忆增强系统（可选，vectordb.enabled=false 时不需要）
chromadb>=0.4.0
fastembed>=0.5.0

# MCP Client（可选，mcp_servers 为空时不需要）
mcp>=1.0.0

# Web 服务器（可选，--web 模式时需要）
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
